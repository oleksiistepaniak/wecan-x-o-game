import {CreateGameParams, CreateGameResponse} from "./BackendService.ts";

export class BackendController {
    async createGame(params: CreateGameParams): Promise<CreateGameResponse> {
        return {

        }
    }
}